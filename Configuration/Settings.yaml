Neos:
  Flow:
    mvc:
      routes:
        'Ewert.WebPush': true
  Neos:
    fusion:
      autoInclude:
        'Ewert.WebPush': true
    modules:
      'management':
        submodules:
          'webPush':
            label: 'Push Messages'
            controller: 'Ewert\WebPush\Controller\MessageController'
            description: 'This module contains an overview of all push messages and allows to add new messages and send, edit or delete existing ones.'
            icon: 'icon-comment-dots'

Ewert:
  WebPush:
    vapid:
      publicKey: ''
      privateKey: ''
